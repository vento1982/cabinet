%h1 Wszystkie dokumenty
- unless @docs.blank?
	- @docs.each do |doc|
		%a{ href: (url_for [doc])}			
			.doc
				%p.title= doc.title
				%p.data= time_ago_in_words(doc.created_at)
				%p.content= truncate(doc.content, lenght: 50)
- else
	%h2 Create Doc!
	%p Start creating documents and organizing Your life!
	%button= link_to "Create Doc", new_doc_path

